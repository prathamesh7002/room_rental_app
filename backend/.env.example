# Environment Variables for Room Rental Platform
# Copy this file to .env and fill in your values
# For production, use .env.production with secure values

# Django Settings
SECRET_KEY=your-secret-key-here-change-this-for-production
DEBUG=True
ENVIRONMENT=development
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
# Development (SQLite - default)
# DATABASE_ENGINE=django.db.backends.sqlite3
# DATABASE_NAME=db.sqlite3

# Production (MySQL - recommended)
DATABASE_ENGINE=django.db.backends.mysql
DATABASE_NAME=if0_39742754_room_rent_app_db
DATABASE_USER=if0_39742754
DATABASE_PASSWORD=nuJNIBF9bSR
DATABASE_HOST=sql201.infinityfree.com
DATABASE_PORT=3306

# Security Settings
SECURE_SSL_REDIRECT=False
SECURE_HSTS_SECONDS=0
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False

# CORS Settings
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
CORS_ALLOW_CREDENTIALS=True

# Email Settings (optional)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password

# Media and Static Files
MEDIA_ROOT=media
STATIC_ROOT=staticfiles

# Redis (for production caching and sessions)
# REDIS_URL=redis://localhost:6379/0
